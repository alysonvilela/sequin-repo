account:
  name: "Playground"

users:
  - account: "Playground"
    email: "admin@sequinstream.com"
    password: "sequinpassword!"

databases:
  - name: "sequin-sinks"
    username: "${PG_DIRECT_USERNAME}"
    password: "${PG_DIRECT_PASSWORD}"
    hostname: "${PG_DIRECT_HOSTNAME}"
    database: "${PG_DIRECT_DATABASE}"
    port: 5432
    slot_name: "sequin_slot"
    publication_name: "sequin_pub"